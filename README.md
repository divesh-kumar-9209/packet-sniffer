# Packet Sniffer (Python)

A simple packet sniffer built using Scapy.

## Features
- Capture live network packets
- Detect protocols (TCP, UDP, ICMP)
- Extract IP addresses and ports
- Display structured output

## Run

Run as administrator:

python main.py